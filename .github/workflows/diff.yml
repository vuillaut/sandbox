
name: diff_files
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: diff
        run: | 
          CHANGED_FILES=$(git diff --name-only)
          echo $CHANGED_FILES

      
